download the npm react app globally
create react app
or npm install g create react

components ass the wrapping component for routing and DB
the import from components is essentially where the app will live
you can type words directly.

from components to properties
The simplest way to define a component is to write a JavaScript function:
function Welcome(props) {
  return <h1>Hello, {props.name}</h1>;
}
props are then the calling of component
const Math = (props) => {
return (
//(props.children)

Yarn.start
runs a preview on the local host
you have to import React from "react" to your active components

SPREAD OPERATORS in react
duplicate property elements cannot be read
when two objects contain the same property
like you add two of the same value
you can't rerender off of this.state ( a handle change)

Bootstrap components in react go in head of public/ index.html
Reactstrap is import reactstrap

Props (short for properties) are a Component's configuration, its options if you may. They are received from above and immutable.


Reactstrap react react-dom
npm install axios react-router-dom
https://reactstrap.github.io/components/alerts/

PUPSTER
pages>about.js,discover.js,search.js
RCS is the boiler for React Components
but where does the router go.

within the SRC we have a components folder and all our shit
if you add a hierarchial folder like wrapper thats fine
then your footer and container and everything has it's own fuckin foldr

import React from "react";
import { Link, Route } from "react-router-dom";
import Learn from "./Learn";

        <Route exact path="/" component={Home} />
import { BrowserRouter as Router, Route } from "react-router-dom";
You’ll need a React web app to add react-router.If you need to create one, the easiest way to get started is with a popular tool called Create React App.


UglifyJS is a JavaScript compressor/minifier written in JavaScript

Review React Map and Key
[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[]]]]]]]]]]]]]]]]]]]]]]]]]]]]

Mongodb book reader
converting to hooks

(On thurs PLEASE REVIEW we will be adding a router to the book app (REDUX< COMPLEX API)